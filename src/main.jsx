import React from "react";
import ReactDOM from "react-dom/client";
import DatasetsApp from "./datasets-app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DatasetsApp />
  </React.StrictMode>
);
